group = 'com.balionis'
version = APP_VERSION

task zip (type: Zip) {
   from 'public/'
   include '**/*' 
   archiveName rootProject.name + '-' + APP_VERSION + '.zip'
   destinationDir(file('./build/'))
}
